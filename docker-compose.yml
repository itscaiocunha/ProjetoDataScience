version: "3"

services:
  gerador-dados:
    build: ./gerador_dados
    volumes:
      - ./data:/data
    container_name: gerador-dados

  analise-r:
    build: ./analise_r
    volumes:
      - ./data:/data
    depends_on:
      - gerador-dados
    container_name: analise-r
